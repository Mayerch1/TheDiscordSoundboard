<UserControl x:Class="DicsordBot.PlaylistSingleView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:DicsordBot"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <ListBox x:Name="list_All" ItemsSource="{Binding PlaylistFiles}">
            <ListBox.ItemTemplate>
                <DataTemplate>

                    <!-- TODO: use something different?-->
                    <Grid Tag="{Binding Id}" MouseLeftButtonDown="stack_list_MouseLeftButtonDown">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>

                        <Grid.ContextMenu>
                            <ContextMenu>
                                <MenuItem Header="Add to Queue"
                                              Tag="{Binding Id}"
                                                  Click="menu_addToQueue_Clicked" />

                                <MenuItem Header="Add to Playlist...">

                                    <MenuItem Header="New Playlist..." />
                                    <MenuItem Header="Test1" />
                                    <MenuItem Header="Test2" />
                                </MenuItem>
                            </ContextMenu>
                        </Grid.ContextMenu>

                        <!-- TODO: spacer, left, middle and right aligned-->

                        <Grid Grid.Column="0">
                            <Label Content="{Binding Name}" />
                        </Grid>

                        <Grid Grid.Column="1">
                            <Label Content="{Binding Author}" />
                        </Grid>

                        <Grid Grid.Column="2">
                            <Label Content="{Binding LengthString}" />
                        </Grid>
                        <!--TODO: button dropdown  -->
                        <Grid Grid.Column="3">
                            <MenuItem Click="menu_openContext_Click" Tag="{Binding Id}">
                                <MenuItem.Icon>
                                    <materialDesign:PackIcon Kind="DotsHorizontal" />
                                </MenuItem.Icon>
                            </MenuItem>
                        </Grid>
                    </Grid>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</UserControl>